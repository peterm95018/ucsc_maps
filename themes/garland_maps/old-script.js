//togg
//Drupal.behaviors.togg = function (context) {
 // $('a#togg-link:not(.togg-processed)', context).addClass('togg-processed').each(function () {
 //   $(this).click(function() {
 //     $("div#togg").toggle(400);
 //     return false;
 //   });
//  });
// };


/*
Drupal.behaviors.bef_live_filter = function() {
  // Hide the apply button.
  $('.views-exposed-form input:submit').hide();

  // When the change event fires, run the submit handler
  $('.views-exposed-form input').change(function() {
    $(this).parents('form').submit();
  });  
}
*/



/*
NOT SURE WHAT I WAS WORKING ON THAT LEFT THIS CRUFT


Drupal.behaviors.togg = function (context) {
	/*
	$(".overlayBox").click(function(event) {
		//	toggleOverlay($(this).attr("checked"), this.id);
			alert(this.id);
	});
	*/
	
	
/*	
$(":checkbox").each(function() {
   this.checked = true;
   this.checked = false; // or, to uncheck
   this.checked = !this.checked; // or, to toggle
 });

jQuery.fn.check = function(mode) {
   // if mode is undefined, use 'off' as default
   var mode = mode || 'off';

   return this.each(function() {
     switch(mode) {
       case 'on':
         this.checked = true;
         break;
       case 'off':
         this.checked = false;
         break;
       case 'toggle':
         this.checked = !this.checked;
         break;
     }
   });
 };
*/
};

*/
